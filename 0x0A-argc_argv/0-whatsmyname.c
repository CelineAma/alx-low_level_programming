#include "main.h"

/**
 * main - entry point
 * @argc: Argument count
 * @argv: Argument vector
 *
 * Return: 0 on success always
*/
int main(int argc, char *argv[])
{
	if (argc > 0)
		printf("%s\n", argv[0]);

	return (0);
}
